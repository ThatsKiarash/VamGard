@model List<LoanType>
@{ ViewData["Title"] = "مدیریت انواع وام"; }

<div class="d-flex justify-content-between align-items-center mb-3">
    <p class="text-muted mb-0">@Model.Count نوع وام</p>
    <a asp-action="Create" class="btn btn-primary"><i class="bi bi-plus-lg"></i> نوع وام جدید</a>
</div>

<div class="card">
    <div class="card-body p-0">
        <table class="table table-hover mb-0">
            <thead class="table-light">
                <tr>
                    <th>#</th>
                    <th>آیکون</th>
                    <th>نام</th>
                    <th>اسلاگ</th>
                    <th>ترتیب</th>
                    <th>وضعیت</th>
                    <th>عملیات</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var lt in Model)
                {
                    <tr>
                        <td>@lt.Id</td>
                        <td><i class="bi @(lt.IconClass ?? "bi-cash-coin") fs-5" style="color: #1a73e8;"></i></td>
                        <td class="fw-bold">@lt.Name</td>
                        <td><code>@lt.Slug</code></td>
                        <td>@lt.DisplayOrder</td>
                        <td>
                            @if (lt.IsActive)
                            {
                                <span class="badge bg-success">فعال</span>
                            }
                            else
                            {
                                <span class="badge bg-danger">غیرفعال</span>
                            }
                        </td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@lt.Id" class="btn btn-sm btn-outline-primary">
                                <i class="bi bi-pencil"></i>
                            </a>
                            <form asp-action="Delete" asp-route-id="@lt.Id" method="post" style="display:inline;" onsubmit="return confirm('آیا مطمئنید؟')">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
